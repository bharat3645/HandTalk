# Copy this file to .env and adjust values for your environment.

# Port for the Node.js/Socket.IO signaling server (index.js)
PORT=3001

# The Python backends (server.py, ser.py, main.py) read their own settings
# directly from the environment (no .env loader there) -- see the "Python
# environment variables" section in the README for MODEL_PATH, LABELS_PATH,
# HAND_MODEL_PATH, CAMERA_INDEX, and PORT.
